#delete before competition. This is for testing the code on the Xavier

from operator import mod


class xavier_testing:
    def print_robot_pose(tx, ty, tz):
        print(f"tx: {tx}")
        print(f"tx: {ty}")
        print(f"tx: {tz}")
    def print_tag_pose(tag_pose):
        print(f"tag pose: {tag_pose}")